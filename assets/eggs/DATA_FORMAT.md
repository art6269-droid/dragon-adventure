# Egg content format

Canonical path:

`assets/eggs/{element}/{rarity}/{0001}/`

Each egg folder contains `data.json`, `icon.png`, and `idle/01.png` through
`idle/06.png`. Rebuild the content catalog after adding a folder.
